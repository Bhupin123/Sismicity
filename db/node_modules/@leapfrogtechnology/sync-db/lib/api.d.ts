/**
 * Programmatic API
 * ----------------
 * This module defines the Programmatic API of sync-db.
 * The functions exposed here are used by the CLI frontend and
 * are also meant to be the public interface for the developers using it as a package.
 */
import { DatabaseConnections } from './util/db';
import Configuration from './domain/Configuration';
import SynchronizeParams from './domain/SynchronizeParams';
import OperationParams from './domain/operation/OperationParams';
import OperationResult from './domain/operation/OperationResult';
/**
 * Synchronize all the configured database connections.
 *
 * @param {Configuration} config
 * @param {DatabaseConnections} conn
 * @param {SynchronizeParams} [options]
 * @returns {Promise<OperationResult[]>}
 */
export declare function synchronize(config: Configuration, conn: DatabaseConnections, options?: SynchronizeParams): Promise<OperationResult[]>;
/**
 * Prune all synchronized objects from the databases (except the ones like tables made via migrations).
 *
 * TODO: An ability to prune only a handful of objects from the last.
 *
 * @param {Configuration} config
 * @param {(DatabaseConnections)} conn
 * @param {OperationParams} [options]
 * @returns {Promise<OperationResult[]>}
 */
export declare function prune(config: Configuration, conn: DatabaseConnections, options?: OperationParams): Promise<OperationResult[]>;
/**
 * Migrate Latest.
 *
 * @param {Configuration} config
 * @param {(DatabaseConnections)} conn
 * @param {OperationParams} [options]
 * @returns {Promise<OperationResult[]>}
 */
export declare function migrateLatest(config: Configuration, conn: DatabaseConnections, options?: OperationParams): Promise<OperationResult[]>;
/**
 * Migrate Rollback.
 *
 * @param {Configuration} config
 * @param {(DatabaseConnections)} conn
 * @param {OperationParams} [options]
 * @returns {Promise<OperationResult[]>}
 */
export declare function migrateRollback(config: Configuration, conn: DatabaseConnections, options?: OperationParams): Promise<OperationResult[]>;
/**
 * List Migrations.
 *
 * @param {Configuration} config
 * @param {(DatabaseConnections)} conn
 * @param {OperationParams} [options]
 * @returns {Promise<OperationResult[]>}
 */
export declare function migrateList(config: Configuration, conn: DatabaseConnections, options?: OperationParams): Promise<OperationResult[]>;
