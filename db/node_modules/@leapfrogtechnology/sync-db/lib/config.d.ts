import Configuration from './domain/Configuration';
import ConnectionConfig from './domain/ConnectionConfig';
/**
 * Check if this is being run via the sync-db cli or not.
 *
 * @returns {boolean}
 */
export declare function isCLI(): boolean;
/**
 * Get the SQL base path - the 'sql' directory under the `basePath`.
 *
 * TODO: Think of a better way later.
 *
 * @param {Configuration} config
 * @returns {string}
 */
export declare function getSqlBasePath(config: Configuration): string;
/**
 * Load config yaml file.
 *
 * @returns {Promise<Configuration>}
 */
export declare function loadConfig(configFilename?: string): Promise<Configuration>;
/**
 * Validate the loaded configuration.
 *
 * @param {Configuration} config
 */
export declare function validate(config: Configuration): void;
/**
 * Resolve database connections.
 *
 * @returns {Promise<ConnectionConfig[]>}
 */
export declare function resolveConnections(config: Configuration, resolver?: string): Promise<ConnectionConfig[]>;
/**
 * Resolve connections using the provided connection resolver.
 *
 * @param {string} resolver
 * @param {Configuration} config
 * @returns {Promise<ConnectionConfig[]>}
 */
export declare function resolveConnectionsUsingResolver(resolver: string, config: Configuration): Promise<ConnectionConfig[]>;
/**
 * Get the connection id from the config.
 *
 * @param {ConnectionConfig} connectionConfig
 * @returns {string}
 */
export declare function getConnectionId(connectionConfig: ConnectionConfig): string;
/**
 * Resolve database connections from Env.
 *
 * @returns {ConnectionConfig[]}
 */
export declare function resolveConnectionsFromEnv(): ConnectionConfig[];
