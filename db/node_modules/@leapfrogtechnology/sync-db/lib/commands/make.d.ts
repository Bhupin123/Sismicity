import { Command, flags } from '@oclif/command';
import MakeOptions from '../domain/MakeOptions';
import Configuration from '../domain/Configuration';
declare class Make extends Command {
    static description: string;
    static args: {
        name: string;
        description: string;
        required: boolean;
    }[];
    static flags: {
        'object-name': flags.IOptionFlag<string | undefined>;
        create: import("@oclif/parser/lib/flags").IBooleanFlag<boolean>;
        config: flags.IOptionFlag<string | undefined>;
        type: flags.IOptionFlag<string>;
    };
    /**
     * CLI command execution handler.
     *
     * @returns {Promise<void>}
     */
    run(): Promise<void>;
    /**
     * Make files based on the given name and type.
     *
     * @param {Configuration} config
     * @param {string} filename
     * @param {string} type
     * @param {string} objectName
     * @returns {Promise<string[]>}
     */
    makeFiles(config: Configuration, filename: string, type?: string, options?: Partial<MakeOptions>): Promise<string[]>;
}
export default Make;
