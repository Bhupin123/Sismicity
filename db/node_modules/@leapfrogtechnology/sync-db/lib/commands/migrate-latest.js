"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const chalk_1 = require("chalk");
const api_1 = require("../api");
const logger_1 = require("../util/logger");
const __1 = require("..");
const io_1 = require("../util/io");
class MigrateLatest extends command_1.Command {
    constructor() {
        super(...arguments);
        /**
         * Started event handler.
         */
        this.onStarted = async (result) => {
            await (0, io_1.printLine)((0, chalk_1.bold)(` ▸ ${result.connectionId}`));
            await (0, io_1.printInfo)('   [✓] Migration - started');
        };
        /**
         * Success handler.
         */
        this.onSuccess = async (result) => {
            const log = (0, logger_1.dbLogger)(result.connectionId);
            const [num, list] = result.data;
            const alreadyUpToDate = num && list.length === 0;
            log('Up to date: ', alreadyUpToDate);
            await (0, io_1.printInfo)(`   [✓] Migration - completed (${result.timeElapsed}s)`);
            if (alreadyUpToDate) {
                await (0, io_1.printInfo)('   Already up to date.\n');
                return;
            }
            // Completed migrations.
            for (const item of list) {
                await (0, io_1.printLine)((0, chalk_1.cyan)(`       - ${item}`));
            }
            await (0, io_1.printInfo)(`   Ran ${list.length} migrations.\n`);
        };
        /**
         * Failure handler.
         */
        this.onFailed = async (result) => {
            await (0, io_1.printLine)((0, chalk_1.bold)((0, chalk_1.red)(`   [✓] Migration - Failed\n`)));
        };
    }
    /**
     * CLI command execution handler.
     *
     * @returns {Promise<void>}
     */
    async run() {
        const { flags: parsedFlags } = this.parse(MigrateLatest);
        const isDryRun = parsedFlags['dry-run'];
        const config = await (0, __1.loadConfig)(parsedFlags.config);
        const connections = await (0, __1.resolveConnections)(config, parsedFlags['connection-resolver']);
        if (isDryRun)
            await (0, io_1.printLine)((0, chalk_1.magenta)('\n• DRY RUN STARTED\n'));
        const results = await (0, api_1.migrateLatest)(config, connections, Object.assign(Object.assign({}, parsedFlags), { onStarted: this.onStarted, onSuccess: this.onSuccess, onFailed: this.onFailed }));
        const failedCount = results.filter(({ success }) => !success).length;
        if (failedCount === 0) {
            if (isDryRun)
                await (0, io_1.printLine)((0, chalk_1.magenta)('• DRY RUN ENDED\n'));
            return process.exit(0);
        }
        (0, io_1.printError)(`Error: Migration failed for ${failedCount} connection(s).`);
        if (isDryRun)
            await (0, io_1.printLine)((0, chalk_1.magenta)('\n• DRY RUN ENDED\n'));
        process.exit(-1);
    }
}
MigrateLatest.description = 'Run the migrations up to the latest changes.';
MigrateLatest.flags = {
    'dry-run': command_1.flags.boolean({ description: 'Dry run migration.', default: false }),
    only: command_1.flags.string({
        helpValue: 'CONNECTION_ID(s)',
        description: 'Filter provided connection(s). Comma separated ids eg: id1,id2'
    }),
    'connection-resolver': command_1.flags.string({
        helpValue: 'PATH',
        description: 'Path to the connection resolver.'
    }),
    config: command_1.flags.string({
        char: 'c',
        description: 'Custom configuration file.'
    })
};
exports.default = MigrateLatest;
