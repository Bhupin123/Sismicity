import MakeOptions from '../domain/MakeOptions';
import Configuration from '../domain/Configuration';
import MakePublishResult from '../domain/MakePublishResult';
/**
 * Copy stub files to user directory and return list of template names.
 *
 * @param  {Configuration} config
 * @returns {Promise<MakePublishResult>}
 */
export declare function publish(config: Configuration): Promise<MakePublishResult>;
/**
 * Generate migration file(s).
 *
 * @param {Configuration} config
 * @param {string} filename
 * @param {Partial<MakeOptions} options
 * @returns {Promise<string[]>}
 */
export declare function makeMigration(config: Configuration, filename: string, options?: Partial<MakeOptions>): Promise<string[]>;
/**
 * Generate sql migration file(s).
 *
 * @param {string} filename
 * @param {MakeOptions} options
 * @returns {Promise<string[]>}
 */
export declare function makeSqlMigration(filename: string, options: Omit<MakeOptions, 'extension'>): Promise<string[]>;
/**
 * Generate JS/TS migration file(s).
 *
 * @param {string} filename
 * @param {MakeOptions} options
 * @returns {Promise<string[]>}
 */
export declare function makeJSMigration(filename: string, options: MakeOptions): Promise<string[]>;
