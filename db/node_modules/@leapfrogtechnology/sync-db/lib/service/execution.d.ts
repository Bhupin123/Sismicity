import Configuration from '../domain/Configuration';
import { Promiser } from '../util/promise';
import OperationResult from '../domain/operation/OperationResult';
import OperationContext from '../domain/operation/OperationContext';
/**
 * Execute a list of processes according to the configuration.
 *
 * @param {Promiser<T>[]} processes
 * @param {Configuration} config
 * @returns {Promise<T[]>}
 */
export declare function executeProcesses<T>(processes: Promiser<T>[], config: Configuration): Promise<T[]>;
/**
 * Execute a unit operation.
 *
 * @param {T} context
 * @param {(options: any) => Promise<any>} func
 * @returns {Promise<OperationResult>}
 */
export declare function executeOperation<T extends OperationContext>(context: T, func: (options: any) => Promise<any>): Promise<OperationResult>;
